ja:
  activerecord:
    models:
      query_string: クエリストリング
      favorite: ブックマーク
    attributes:
      query_string:
        description: 説明
    errors:
      models:
        query_string:
          attributes:
            description:
              format: '%{message}'
        favorite:
          attributes:
            query_string_id:
              format: '%{message}'
  attributes:
    created_at: 作成日
    updated_at: 更新日
